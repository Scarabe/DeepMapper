server:
  port: 0

endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: false

eureka:
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka-service.credentials.uri:http://192.168.11.200:8763}/eureka/
      
ribbon:
  eureka:
    enabled: true
    
spring:
  application:
    name: finchnlvisual